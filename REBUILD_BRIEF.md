# 豆包语音对话项目重构整理

## 1. 项目目标

当前项目是一个本地运行的语音对话应用，目标是把用户语音输入串成完整链路：

1. 浏览器录音
2. 豆包 ASR 实时识别
3. QWEN 流式生成回复
4. 豆包 TTS 播报回复
5. 页面上同步展示实时识别文本、对话记录和状态

一句话概括：**语音输入 → ASR → LLM → TTS → 页面回显与播报**。

---

## 2. 当前已有功能

### 2.1 语音识别（ASR）
- 通过浏览器麦克风采集音频。
- 前端将 PCM 音频流通过本地代理转发给豆包 ASR WebSocket。
- 支持实时返回中间识别结果。
- 支持 definite utterance 后自动触发后续大模型处理。

### 2.2 大模型对话（QWEN）
- 调用阿里云 DashScope 的 QWEN 接口。
- 支持流式返回。
- 支持多模型切换：
  - `qwen3.6-max-preview`
  - `qwen3.6-plus`
  - `qwen3.6-flash`
- 保存一份本地会话历史（system / user / assistant）。

### 2.3 语音合成（TTS）
- 通过本地代理连接豆包 TTS WebSocket。
- 支持多音色选择。
- 支持边生成边播报的会话式状态管理。
- 支持中断播报。

### 2.4 实时打断能力
- 当用户再次说话时，如果当前正在 QWEN 输出或 TTS 播报，会尝试中断当前输出。

### 2.5 本地设置面板
- 页面右上角有设置面板。
- 可以查看/编辑以下配置：
  - QWEN API Key
  - QWEN 默认模型
  - 豆包 App ID
  - 豆包 Access Token
  - 豆包 API Key
  - ASR Resource ID
  - TTS Resource ID
  - 代理地址
- 设置可通过本地代理写回 `.env`。

### 2.6 状态与延迟展示
- 页面有状态栏，显示当前状态：就绪 / 检查 QWEN / 连接 ASR / 录音中 / 思考中 / 播报中 / 错误。
- 展示链路耗时，例如：
  - 识别收尾
  - QWEN 首字
  - QWEN 完成
  - 进入播报
  - TTS 播放
  - 总耗时

### 2.7 对话区与实时文本区
- 页面显示用户消息和助手消息。
- 页面底部实时显示 ASR 当前识别文本。
- 支持清空对话。

### 2.8 重写原则
- 除 UI 之外，现有功能代码不作为复用目标。
- 功能层、流程层、状态层、代理层、配置层都按重建思路处理。
- 这份文档描述的是“需要保留的产品能力”和“需要保留的 UI 效果”，不是旧代码复用说明。
- 不需要按旧文件、旧函数、旧实现逐段迁移。

相关现状参考文件：
- `index.html`
- `src/style.css`
- `src/main.js`
- `src/asr.js`
- `src/qwen.js`
- `src/tts.js`
- `server.js`
- `README.md`

这些文件仅用于理解现有功能与界面，不代表重构时要沿用其中的功能代码。

---

## 3. 当前页面结构

页面整体由以下几部分组成：

1. **Header**
   - 标题：豆包语音对话
   - 副标题：语音输入 → 豆包识别 → QWEN 回答 → TTS 播报
   - 设置按钮

2. **设置面板**
   - 大模型设置
   - 豆包设置
   - 保存按钮
   - 设置状态提示

3. **主控制区**
   - LLM 模型选择
   - 豆包音色选择
   - 开始录音
   - 停止录音
   - 清空对话

4. **聊天区**
   - 用户消息气泡
   - 助手消息气泡

5. **状态栏**
   - 状态文本
   - 延迟指标

6. **实时文本区**
   - 显示 ASR 当前识别结果

对应文件：
- `index.html`

---

## 4. 当前 UI 风格总结

### 4.1 视觉风格
当前 UI 风格很明确，属于：

- 白底
- 高对比黑边框
- 明亮黄色强调色
- 扁平化
- 直角矩形
- 无阴影、无圆角、无渐变

### 4.2 视觉规则
从 `src/style.css` 可以总结出以下设计语言：

- 主背景：纯白 `#ffffff`
- 主强调色：亮黄 `#ffd600`
- 文本主色：近黑 `#111111`
- 线条：纯黑边框 `#000000`
- 危险色：红色 `#d84b4b`
- 所有面板均使用：
  - `2px solid black`
  - `border-radius: 0`
- 所有按钮和输入框均为直角
- 用户消息背景是黄色
- 助手消息背景是白色
- 状态栏整体是黄色底

### 4.3 布局风格
- 桌面端使用大宽度容器：`min(1280px, 96vw)`
- 控制区使用多列 grid
- 移动端收缩为单列
- 聊天区有固定最小高度

### 4.4 UI 重构边界
这套 UI 现在就是重构参考样式，**可以直接保留现有 UI 代码**，不把 UI 当作重做范围。

建议直接复用：
- `index.html`
- `src/style.css`

如果后续重构，优先重做的是：
- 业务流程
- 状态管理
- 模块边界
- 代理服务
- 配置机制

而不是视觉层。

这和你记忆里的偏好也一致：**方角、纯白、纯色、无渐变**。

可保留的视觉方向：
- 纯白背景
- 方角面板
- 扁平按钮
- 黄色作为主品牌强调色
- 对话气泡的黄 / 白对比

---

## 5. 当前技术架构

### 5.1 前端
- Vite
- 原生 HTML / CSS / JS
- 无框架

相关文件：
- `package.json`
- `index.html`
- `src/main.js`

### 5.2 后端 / 本地代理
- Node.js
- `ws` WebSocket 库
- `http` 模块
- 一个本地代理服务同时承担两类职责：
  1. 转发豆包 ASR / TTS WebSocket 请求
  2. 提供 `/config` 接口读写 `.env`

相关文件：
- `server.js`

### 5.3 第三方能力
- 豆包 ASR WebSocket
- 豆包 TTS WebSocket
- 阿里云 QWEN HTTP Stream

---

## 6. 当前核心流程

### 6.1 页面初始化
- 初始化模型选择器
- 初始化音色选择器
- 初始化设置面板
- 读取本地配置
- 应用到 QWEN / ASR / TTS 实例

### 6.2 开始录音流程
`startRecording()` 当前流程：

1. 打断当前输出
2. 记录计时
3. 检查 QWEN 可用性
4. 获取麦克风权限
5. 连接 ASR 代理
6. 发送 ASR full client request
7. 创建 AudioContext 与 ScriptProcessor
8. 持续发送音频 PCM 数据
9. 更新 UI 为“正在录音”

### 6.3 ASR 返回文本
- 中间文本更新到 `realtimeText`
- definite 文本触发 `handleUserMessage()`
- 如当前正在播报/思考，可能触发打断

### 6.4 大模型处理
- 将 definite 文本作为用户消息加入聊天区
- 创建助手消息占位
- 调用 QWEN 流式生成
- 实时更新助手消息内容

### 6.5 TTS 播报
- QWEN 完成后进入 TTS
- 使用当前选中的音色
- 完成后恢复“就绪”

### 6.6 停止录音
- 停止当前输出
- 关闭录音流
- 关闭 AudioContext
- 发送 ASR 结束包
- 关闭 ASR 连接
- 清理 UI 状态

---

## 7. 当前配置项

### 7.1 QWEN 配置
- `VITE_ALYUN_API_KEY`
- `VITE_DASHSCOPE_API_KEY`
- `VITE_QWEN_MODEL`

### 7.2 豆包配置
- `VITE_VOLC_APP_ID`
- `VITE_VOLC_ACCESS_TOKEN`
- `VITE_VOLC_API_KEY`
- `VITE_VOLC_RESOURCE_ID`
- `VITE_VOLC_TTS_RESOURCE_ID`
- `VITE_VOLC_PROXY_URL`

### 7.3 本地代理
- 默认端口：`3001`
- 支持通过 `PORT` 环境变量覆盖

配置现状参考文件：
- `server.js`
- `README.md`

---

## 8. 当前已知问题

### 8.1 豆包 ASR 鉴权 / 连接问题
当前最明显的问题是：
- 浏览器可以连到本地代理
- 本地代理连上游豆包 ASR 时返回 `401/403`
- 说明问题在“本地代理 → 豆包 ASR”这段

### 8.2 代理进程与前端联调容易混乱
- Vite 页面刷新不会自动重启 Node 代理
- 容易出现“前端已更新、代理还是旧版本”的情况
- 重构时建议把开发体验重新梳理

### 8.3 主流程耦合较重
当前主流程承担了过多职责：
- DOM 管理
- 配置管理
- 状态管理
- 录音流程
- ASR 流程
- QWEN 流程
- TTS 流程
- 打断逻辑
- 延迟展示

这会让后续维护、重构、排错成本很高。

### 8.4 模块边界不够清晰
虽然现有代码已经按 ASR / QWEN / TTS 做了基础拆分，但主调度逻辑仍然高度集中。

### 8.5 调试信息已经开始侵入主流程
为排查 403，已经往代理和前端里塞了不少调试日志；如果决定重构，建议不要直接沿用现在的调试状态，而是重新设计：
- 日志等级
- 错误展示方式
- 调试信息面板

---

## 9. 重构建议

### 9.1 重构目标
当前最合适的目标不是修补旧功能代码，而是：

- **保留现有 UI**
- **保留现有产品能力**
- **功能代码全部重写**
- 用新的模块边界和状态结构重建主流程

也就是说，这次重构不要求沿用旧实现，只要求保留：
- 页面样式
- 页面结构
- 产品交互目标
- 配置项语义

### 9.2 推荐的重构模块划分
建议把新前端拆成以下层次：

#### 1. `app` 层
负责应用初始化和模块装配。

#### 2. `state` 层
统一管理：
- 当前配置
- 录音状态
- 识别状态
- 对话状态
- 播报状态
- 当前 runId
- 错误状态

#### 3. `services` 层
- `asrService`
- `qwenService`
- `ttsService`
- `configService`

#### 4. `controllers` 层
- `recordingController`
- `conversationController`
- `settingsController`
- `interruptController`

#### 5. `ui` 层
- `chatView`
- `statusView`
- `settingsView`
- `controlsView`
- `realtimeTextView`

这样可以避免新主入口再次变成总控大文件。

### 9.3 推荐的代理重构方向
当前代理同时处理：
- 配置 API
- ASR 代理
- TTS 代理
- 调试日志

建议新代理拆分为：
- `config routes`
- `volc auth/header builder`
- `proxy connection factory`
- `error translator`

最少也应该把以下逻辑拆出去：
- 配置读写
- 鉴权头生成
- WebSocket 转发
- 错误格式化

### 9.4 推荐的 UI 处理方式
UI 不重做，直接复用现有：
- 页面结构
- 样式文件
- 视觉语言

只对功能接线重新实现，不把精力放在改视觉上。

### 9.5 推荐先做的最小可运行版本（MVP）
如果你要重新构建，建议先不要一次性把全部问题都解决，而是按这个顺序做：

#### 第一步：最小主流程跑通
- 保留现有 UI
- 重建录音按钮逻辑
- 重建 ASR 接入
- 重建文本展示

#### 第二步：加 QWEN
- 把 definite 文本送入 QWEN
- 展示流式回复

#### 第三步：加 TTS
- 播报 QWEN 结果

#### 第四步：加设置系统
- 本地配置读取保存
- 设置面板接线

#### 第五步：加打断与延迟统计
- 输出打断
- 耗时分析

这样能避免一上来就被完整链路卡死。

---

## 10. 建议保留的产品体验

如果重构，以下体验建议保留：

1. **一键开始录音**
2. **实时识别文本可见**
3. **识别完成自动触发大模型**
4. **模型回复流式展示**
5. **回复自动播报**
6. **用户说话时可打断当前播报**
7. **状态和耗时清晰可见**
8. **本地设置可直接保存，不依赖手改 `.env`**

这些是当前项目里最有价值的部分。

---

## 11. 当前代码文件职责总览

### 需要直接保留的部分
- `index.html`
  - 页面结构
- `src/style.css`
  - 全部视觉与布局样式

### 仅用于参考、不建议复用实现的部分
- `src/main.js`
  - 当前主流程与页面接线
- `src/asr.js`
  - 当前 ASR 实现
- `src/qwen.js`
  - 当前 QWEN 实现
- `src/tts.js`
  - 当前 TTS 实现
- `server.js`
  - 当前本地代理实现
- `README.md`
  - 当前运行说明

---

## 12. 重构时建议产出物

如果你要彻底重建，建议第一批产出以下文件：

1. `docs/rebuild-scope.md`
   - 重构范围
2. `docs/flow.md`
   - 语音链路与状态图
3. `docs/config.md`
   - 配置项说明
4. `src/app/`
   - 新应用入口
5. `src/services/`
   - ASR/QWEN/TTS/config 服务层
6. `src/ui/`
   - UI 组件层
7. `src/state/`
   - 状态层

---

## 13. 结论

这个项目当前已经具备一个完整语音 Agent Demo 的雏形，价值点很明确：

- 有完整的语音输入到语音输出链路
- 有实时识别、流式回复、语音播报
- 有本地配置面板
- 有打断机制
- 有状态与耗时展示

但如果你现在决定重建，最合理的策略不是继续沿着旧功能代码演进，而是：

**保留 UI 与产品目标，功能代码全部重写。**

也就是说：
- UI 直接留用
- 功能逻辑重新设计
- 状态重新建模
- 代理重新实现
- 配置机制重新梳理

这样才最符合你现在的重构方向。
---

## 3. 当前页面结构

页面整体由以下几部分组成：

1. **Header**
   - 标题：豆包语音对话
   - 副标题：语音输入 → 豆包识别 → QWEN 回答 → TTS 播报
   - 设置按钮

2. **设置面板**
   - 大模型设置
   - 豆包设置
   - 保存按钮
   - 设置状态提示

3. **主控制区**
   - LLM 模型选择
   - 豆包音色选择
   - 开始录音
   - 停止录音
   - 清空对话

4. **聊天区**
   - 用户消息气泡
   - 助手消息气泡

5. **状态栏**
   - 状态文本
   - 延迟指标

6. **实时文本区**
   - 显示 ASR 当前识别结果

对应文件：
- `index.html`

---

## 4. 当前 UI 风格总结

### 4.1 视觉风格
当前 UI 风格很明确，属于：

- 白底
- 高对比黑边框
- 明亮黄色强调色
- 扁平化
- 直角矩形
- 无阴影、无圆角、无渐变

### 4.2 视觉规则
从 `src/style.css` 可以总结出以下设计语言：

- 主背景：纯白 `#ffffff`
- 主强调色：亮黄 `#ffd600`
- 文本主色：近黑 `#111111`
- 线条：纯黑边框 `#000000`
- 危险色：红色 `#d84b4b`
- 所有面板均使用：
  - `2px solid black`
  - `border-radius: 0`
- 所有按钮和输入框均为直角
- 用户消息背景是黄色
- 助手消息背景是白色
- 状态栏整体是黄色底

### 4.3 布局风格
- 桌面端使用大宽度容器：`min(1280px, 96vw)`
- 控制区使用多列 grid
- 移动端收缩为单列
- 聊天区有固定最小高度

### 4.4 UI 重构边界
这套 UI 现在就是重构参考样式，**可以直接保留现有 UI 代码**，不把 UI 当作重做范围。

建议直接复用：
- `index.html`
- `src/style.css`

如果后续重构，优先重做的是：
- 业务流程
- 状态管理
- 模块边界
- 代理服务
- 配置机制

而不是视觉层。

这和你记忆里的偏好也一致：**方角、纯白、纯色、无渐变**。

可保留的视觉方向：
- 纯白背景
- 方角面板
- 扁平按钮
- 黄色作为主品牌强调色
- 对话气泡的黄 / 白对比

---

## 5. 当前技术架构

### 5.1 前端
- Vite
- 原生 HTML / CSS / JS
- 无框架

相关文件：
- `package.json`
- `index.html`
- `src/main.js`

### 5.2 后端 / 本地代理
- Node.js
- `ws` WebSocket 库
- `http` 模块
- 一个本地代理服务同时承担两类职责：
  1. 转发豆包 ASR / TTS WebSocket 请求
  2. 提供 `/config` 接口读写 `.env`

相关文件：
- `server.js`

### 5.3 第三方能力
- 豆包 ASR WebSocket
- 豆包 TTS WebSocket
- 阿里云 QWEN HTTP Stream

---

## 6. 当前核心流程

### 6.1 页面初始化
- 初始化模型选择器
- 初始化音色选择器
- 初始化设置面板
- 读取本地配置
- 应用到 QWEN / ASR / TTS 实例

主要逻辑：
- `src/main.js:438-486`
- `src/main.js:214-247`

### 6.2 开始录音流程
`startRecording()` 当前流程：

1. 打断当前输出
2. 记录计时
3. 检查 QWEN 可用性
4. 获取麦克风权限
5. 连接 ASR 代理
6. 发送 ASR full client request
7. 创建 AudioContext 与 ScriptProcessor
8. 持续发送音频 PCM 数据
9. 更新 UI 为“正在录音”

主要逻辑：
- `src/main.js:503-598`

### 6.3 ASR 返回文本
- 中间文本更新到 `realtimeText`
- definite 文本触发 `handleUserMessage()`
- 如当前正在播报/思考，可能触发打断

主要逻辑：
- `src/main.js:550-580`
- `src/main.js:380-386`

### 6.4 大模型处理
- 将 definite 文本作为用户消息加入聊天区
- 创建助手消息占位
- 调用 QWEN 流式生成
- 实时更新助手消息内容

主要逻辑：
- `src/main.js:631-701`
- `src/qwen.js`

### 6.5 TTS 播报
- QWEN 完成后进入 TTS
- 使用当前选中的音色
- 完成后恢复“就绪”

主要逻辑：
- `src/main.js:670-682`
- `src/tts.js`

### 6.6 停止录音
- 停止当前输出
- 关闭录音流
- 关闭 AudioContext
- 发送 ASR 结束包
- 关闭 ASR 连接
- 清理 UI 状态

主要逻辑：
- `src/main.js:600-629`

---

## 7. 当前配置项

### 7.1 QWEN 配置
- `VITE_ALYUN_API_KEY`
- `VITE_DASHSCOPE_API_KEY`
- `VITE_QWEN_MODEL`

### 7.2 豆包配置
- `VITE_VOLC_APP_ID`
- `VITE_VOLC_ACCESS_TOKEN`
- `VITE_VOLC_API_KEY`
- `VITE_VOLC_RESOURCE_ID`
- `VITE_VOLC_TTS_RESOURCE_ID`
- `VITE_VOLC_PROXY_URL`

### 7.3 本地代理
- 默认端口：`3001`
- 支持通过 `PORT` 环境变量覆盖

相关文件：
- `server.js:8-24`
- `README.md:21-60`

---

## 8. 当前已知问题

### 8.1 豆包 ASR 鉴权 / 连接问题
当前最明显的问题是：
- 浏览器可以连到本地代理
- 本地代理连上游豆包 ASR 时返回 `401/403`
- 说明问题在“本地代理 → 豆包 ASR”这段

涉及文件：
- `server.js`
- `src/asr.js`

### 8.2 代理进程与前端联调容易混乱
- Vite 页面刷新不会自动重启 Node 代理
- 容易出现“前端已更新、代理还是旧版本”的情况
- 重构时建议把开发体验重新梳理

### 8.3 主流程耦合较重
`src/main.js` 当前同时负责：
- DOM 管理
- 配置管理
- 状态管理
- 录音流程
- ASR 流程
- QWEN 流程
- TTS 流程
- 打断逻辑
- 延迟展示

这会让后续维护、重构、排错成本很高。

### 8.4 模块边界不够清晰
虽然拆成了 `asr.js / qwen.js / tts.js`，但主调度逻辑仍然大量集中在 `main.js`。

### 8.5 调试信息已经开始侵入主流程
为排查 403，已经往代理和前端里塞了不少调试日志；如果决定重构，建议不要直接沿用现在的调试状态，而是重新设计：
- 日志等级
- 错误展示方式
- 调试信息面板

---

## 9. 重构建议

### 9.1 先明确重构目标
建议你先决定这次“重新构建代码”的目标是以下哪一种：

#### 方案 A：保留现有功能，重写结构
适合现在。
目标是：
- 功能基本不变
- UI 风格延续
- 重构架构和模块边界
- 清理状态管理
- 重新梳理代理和配置机制

#### 方案 B：连产品交互一起重设计
目标是：
- 重做页面信息架构
- 调整操作方式
- 增加调试面板 / 会话面板 / 设备状态区
- 重新设计完整使用流程

如果你只是要先把项目救活，建议先走 **方案 A**。

### 9.2 推荐的重构模块划分
建议把前端拆成以下层次：

#### 1. `app` 层
负责应用初始化和模块装配。

#### 2. `state` 层
统一管理：
- 当前配置
- 录音状态
- 识别状态
- 对话状态
- 播报状态
- 当前 runId
- 错误状态

#### 3. `services` 层
- `asrService`
- `qwenService`
- `ttsService`
- `configService`

#### 4. `controllers` 层
- `recordingController`
- `conversationController`
- `settingsController`
- `interruptController`

#### 5. `ui` 层
- `chatView`
- `statusView`
- `settingsView`
- `controlsView`
- `realtimeTextView`

这样可以避免 `main.js` 继续变成总控大文件。

### 9.3 推荐的代理重构方向
当前 `server.js` 同时处理：
- 配置 API
- ASR 代理
- TTS 代理
- 调试日志

建议拆分为：
- `config routes`
- `volc auth/header builder`
- `proxy connection factory`
- `error translator`

最少也应该把以下逻辑拆出去：
- 配置读写
- 鉴权头生成
- WebSocket 转发
- 错误格式化

### 9.4 推荐的 UI 重构方向
建议保留现有风格，但优化结构：

#### 保留
- 白底
- 黄黑对比
- 方角
- 扁平
- 大按钮
- 简洁结构

#### 优化
- 顶部 Header 更清晰
- 设置面板改为抽屉或独立卡片组
- 状态区和实时文本区更明确分层
- 对话区支持更稳的流式更新体验
- 把“系统状态 / 错误 / 调试信息”从聊天区剥离

### 9.5 推荐先做的最小可运行版本（MVP）
如果你要重新构建，建议先不要一次性把全部问题都解决，而是按这个顺序做：

#### 第一步：最小主流程跑通
- 页面基础结构
- 录音按钮
- ASR 接入
- 文本展示

#### 第二步：加 QWEN
- 把 definite 文本送入 QWEN
- 展示流式回复

#### 第三步：加 TTS
- 播报 QWEN 结果

#### 第四步：加设置系统
- 本地配置读取保存
- 设置面板

#### 第五步：加打断与延迟统计
- 输出打断
- 耗时分析

这样能避免一上来就被完整链路卡死。

---

## 10. 建议保留的产品体验

如果重构，以下体验建议保留：

1. **一键开始录音**
2. **实时识别文本可见**
3. **识别完成自动触发大模型**
4. **模型回复流式展示**
5. **回复自动播报**
6. **用户说话时可打断当前播报**
7. **状态和耗时清晰可见**
8. **本地设置可直接保存，不依赖手改 `.env`**

这些是当前项目里最有价值的部分。

---

## 11. 当前代码文件职责总览

### 页面与样式
- `index.html`
  - 页面结构
- `src/style.css`
  - 全部视觉与布局样式

### 主逻辑
- `src/main.js`
  - 应用启动
  - 配置初始化
  - 录音控制
  - ASR 回调处理
  - QWEN 调用
  - TTS 播放
  - 打断逻辑
  - UI 更新
  - 耗时统计

### 能力模块
- `src/asr.js`
  - 豆包 ASR 客户端封装
- `src/qwen.js`
  - QWEN HTTP 流式对话封装
- `src/tts.js`
  - 豆包 TTS 客户端封装

### 本地代理
- `server.js`
  - 本地代理服务
  - 配置接口 `/config`
  - 豆包 ASR/TTS WebSocket 转发
  - `.env` 读写

### 文档
- `README.md`
  - 项目简介与运行方式

---

## 12. 重构时建议产出物

如果你要彻底重建，建议第一批产出以下文件：

1. `docs/rebuild-scope.md`
   - 重构范围
2. `docs/flow.md`
   - 语音链路与状态图
3. `docs/config.md`
   - 配置项说明
4. `src/app/`
   - 新应用入口
5. `src/services/`
   - ASR/QWEN/TTS/config 服务层
6. `src/ui/`
   - UI 组件层
7. `src/state/`
   - 状态层

---

## 13. 结论

这个项目当前已经具备一个完整语音 Agent Demo 的雏形，价值点很明确：

- 有完整的语音输入到语音输出链路
- 有实时识别、流式回复、语音播报
- 有本地配置面板
- 有打断机制
- 有状态与耗时展示

但目前的问题也很明确：

- 主流程耦合高
- 调试复杂
- 代理逻辑和配置逻辑混在一起
- 豆包鉴权链路还没彻底稳定

如果你要重构，最合理的策略不是继续在当前结构上补丁式修，而是：

**保留功能目标与视觉语言，重做模块边界、状态管理和代理结构。**
